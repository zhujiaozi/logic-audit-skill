---
name: logic-audit
description: >-
  对推理过程进行深度逻辑审查、查找谬误、纠正思考错误。
  当用户要求"逻辑审计""检查推理漏洞""修正思考链""查逻辑错误""帮我审一下这段推理"时，务必调用此 Skill。
  也适用于用户给出了带编号或不带编号的多步推理、论证、因果分析、数学推导、决策链，需要系统性检查前提真实性、步骤完整性、因果混淆、循环论证、数值错误、以偏概全、非黑即白、稻草人谬误、诉诸情感/权威、滑坡谬误等 13 类常见逻辑问题。
  若用户只是简单提问、单步问答、或无显式推理链的请求，则不需要触发（由 System Prompt 的三步自检覆盖轻量场景）。
version: 1.1
---

# Logic Audit Skill — 逻辑审计

对用户提供的推理链（思考链）进行系统性逻辑审查，输出带编号的审计报告和修正后的思考链。

---

## 触发条件

### 应当触发
- 用户明确要求"审计""查逻辑""检查漏洞""修正推理"等
- 用户给出了带编号或不带编号的多步推理过程并要求检查
- 推理涉及高利害决策、数学证明、因果分析、复杂论证
- 用户对之前的回答不满意，要求深度审查其推理

### 不触发
- 日常对话、单步问答
- 无显式推理链的简单请求
- 仅需 System Prompt 轻量自检即可的场景

---

## 核心审计清单

审计时逐项扫描输入推理链，每项发现需记录：**步骤编号**、**问题类型**、**严重度**、**具体说明**。

| # | 类别 | 检查点 | 说明 |
|---|------|--------|------|
| 1 | **前提真实性** | 每个前提是否有可靠来源、公认事实或合理假设？ | 标记无根据的断言、虚假前提、隐藏假设。 |
| 2 | **步骤完整性** | 推理是否跳过必要的中间步骤？ | 指出缺失的推导环节，要求补全。 |
| 3 | **因果混淆** | 是否将相关当作因果？因果方向是否倒置？ | 例：把同时发生的事件判为因果，或忽略第三变量（混杂因素）。 |
| 4 | **循环论证** | 结论是否已隐含在前提中，或以不同表述重复前提？ | 典型形式："A 因为 A"。 |
| 5 | **数值/计算错误** | 任何算术、统计、概率计算是否准确？ | 重新计算并给出正确结果。 |
| 6 | **以偏概全** | 是否用个别案例、小样本推出普遍结论？ | 指出样本代表性不足。 |
| 7 | **非黑即白** | 是否只给出两个极端选项，忽略中间或替代可能？ | 补充遗漏选项。 |
| 8 | **稻草人谬误** | 是否歪曲对方观点后攻击之？ | 指出曲解处并还原原观点。 |
| 9 | **诉诸情感/权威** | 是否用情感渲染或无关权威代替逻辑论证？ | 标记并说明为何无效。 |
| 10 | **滑坡谬误** | 是否断言第一步必然导致极端连锁后果，而无充分证据？ | 评估每一步的概率和连接合理性。 |
| 11 | **偷换概念** | 同一关键词在推理不同步骤中是否被赋予不同含义？ | 标记词义漂移处，要求统一使用同一概念。 |
| 12 | **诉诸自然** | 是否将"天然/自然"直接等同于"正确/更好/安全"？ | 自然≠安全/有效，需补充具体证据而非诉诸自然标签。 |
| 13 | **合成/分割谬误** | 是否将部分的属性直接推给整体（合成），或将整体的属性直接推给部分（分割）？ | 例：球队每位球员优秀→球队必然优秀（合成）；某公司规模大→其每个部门都规模大（分割）。 |

---

## 执行流程

### 1. 输入解析

期望输入格式为带编号的步骤列表，如：
```
1. 所有 A 都是 B。
2. 这个物体是 B。
3. 所以它是 A。
```

- 若输入为纯文本段落，Skill 先自动提取步骤要点并编号。
- 若完全无法解析为推理链（如仅一句断言），返回：
  > "无法审计：输入内容未构成可审计的推理链，请提供包含前提和结论的多步思考过程。"

### 2. 静默审计

遍历审计清单，对每一步逐条检查，记录所有发现：
- 问题定位到具体步骤编号
- 标注问题类型（对应清单 #1-#13）
- 附上具体说明

此过程为内部推理，不单独输出中间结果。

### 3. 生成修正链

- **保留**原始推理的正确部分。
- 针对每个问题，在原位置进行修正（修正前提、补全跳步、更正谬误等）。
- 修正后的思考链仍以带编号步骤呈现，语言与原始链风格一致。
- 如果原始推理完全错误，生成一条全新的正确推理链。

### 4. 输出报告

按第 5 节格式返回，包含审计结果和修正链。

若审计无问题，输出简洁版：
> "✅ 未发现逻辑错误"
> （随后原样输出步骤）

---

## 输出格式规范

每条审计发现需标注严重度：
- 🔴 **严重** — 推理直接崩溃（前提虚假、计算错误、循环论证等，足以推翻结论）
- 🟡 **可疑** — 推理存疑（证据不足、以偏概全、跳步等，削弱但未必推翻结论）
- ⚪ **轻微** — 小瑕疵（表述模糊、缺少来源引用等，不影响结论但值得改进）

```
### 审计报告
- **步骤 [编号]** | 🔴 [问题类型] — 具体说明
  示例：步骤 2 | 🔴 前提缺失 — 未提供"该物体是 A"的任何证据。
- **步骤 [编号]** | 🟡 [问题类型] — 具体说明
  示例：步骤 5 | 🟡 以偏概全 — 仅凭 2 个案例推出普遍结论，样本不足。
（若多项问题，逐条列出；若无问题，写"无"）

### 修正后的思考链
1. [修正步骤 1]
2. [修正步骤 2]
...

### 附加建议（可选）
[对今后类似推理的改进建议]
```

---

## 边界情况与错误处理

| 场景 | 处理方式 |
|------|----------|
| **输入过短 / 无实质推理** | 返回提示："输入内容未构成可审计的推理链，请提供包含前提和结论的多步思考过程。" |
| **格式混乱** | 先自动提取步骤；若失败则提示用户按 `1. ... 2. ...` 格式重写。 |
| **审计无问题** | 返回 "✅ 未发现逻辑错误"，原样输出步骤。 |
| **自我引用循环**（审计对象为 AI 生成内容） | 在报告开头注明："审计对象为 AI 生成内容，原始推理可能基于概率模型，部分前提来自训练数据，无法完全验证事实性。" 同时增加子步骤：② 检查是否存在**幻觉式推理**（编造引用、虚构数据、张冠李戴的归因）——逐一验证推理中引用的数字、来源、引用是否为真实存在；标记任何无法确认的引用为 🔴 严重 - 信息不可追溯。 |
| **输入为外语推理** | 按原文语言输出审计报告和修正链，保持语言一致。 |

---

## 与 System Prompt 轻量自检的协同

- **日常场景**：System Prompt 中的"三步快速自检"在 AI 生成回答前自动执行，保证 90% 常见谬误被当场修正，响应快且无额外工具调用。
- **深度审查**：当用户对回答不满意、主动要求审计，或推理涉及高利害决策时，调用本 Skill，输出详尽报告。
- **融合流程**：
  1. 用户提问 → AI 用轻量自检生成初始答案
  2. 用户质疑或要求审计 → 触发 logic-audit Skill，对刚生成的思考链进行审计
  3. 返回修正版思考链

整个对话过程自然衔接，上下文连续，不打断体验。
